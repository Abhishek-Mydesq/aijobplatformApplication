server:
  port: 8084
spring:
  application:
    name: resume-service
  datasource:
    url: jdbc:mysql://localhost:3306/resume_db
    username: root
    password: abes290@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka